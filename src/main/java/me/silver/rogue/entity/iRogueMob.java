package me.silver.rogue.entity;

public interface iRogueMob {

    void setup(double x, double y, double z, boolean isBaby, double health, double speed, double attackDamage, Object... options);
}
